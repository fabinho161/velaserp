{
  "name": "velaserp-functions",
  "description": "Backend seguro do Renovar ERP para integrações futuras.",
  "private": true,
  "main": "src/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "node --check src/index.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.1",
    "mercadopago": "^2.0.15"
  }
}
